<%= form_with(model: checkedout_book, local: true) do |form| %>
  <% if checkedout_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(checkedout_book.errors.count, "error") %> prohibited this checkedout_book from being saved:</h2>

      <ul>
      <% checkedout_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
