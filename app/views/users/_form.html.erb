<%= form_with(model: user, local: true) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :id, :value => user[:id], :readonly => true  %>
  </div>
  <br/>

  <div class="field">
      <%= f.label "Name" %><br />
      <%= f.text_field :name, :value => user[:name]  %>
  </div>
  <br/>

  <div class="field">
      <%= f.label "Email [readonly]" %><br/>
      <%= user[:email] %>
  </div>
  <br/>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
