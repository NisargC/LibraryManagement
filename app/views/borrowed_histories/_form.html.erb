<%= form_with(model: borrowed_history, local: true) do |form| %>
  <% if borrowed_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(borrowed_history.errors.count, "error") %> prohibited this borrowed_history from being saved:</h2>

      <ul>
      <% borrowed_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
